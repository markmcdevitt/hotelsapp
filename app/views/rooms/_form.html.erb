<%= form_for(@room) do |f| %>
  <% if @room.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@room.errors.count, "error") %> prohibited this room from being saved:</h2>

      <ul>
      <% @room.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :hotel_id %><br />
	<%= f.collection_select :hotel_id, Hotel.all, :id, :name %>
  </div>
  <div class="field">
    <%= f.label :"Type of room" %><br />
    <%= f.select :category, ["Deluxe Rooms", "Standard Room", "Suite"] %>
  </div>
  <div class="field">
    <%= f.label :price %><br />
    <%= f.text_field :price %>
  </div>
  <div class="field">
    <%= f.label :extras %><br />
    <%= f.text_field :extras %>
  </div>
  <div class="field">
    <%= f.label :"Picture of the room" %><br />
    <%= f.text_field :roomImage %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
