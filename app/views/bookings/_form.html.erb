<%= form_for(@booking) do |f| %>
  <% if @booking.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@booking.errors.count, "error") %> prohibited this booking from being saved:</h2>

      <ul>
      <% @booking.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :room_id %><br />
    <%= f.number_field :room_id %>
  </div>
  <div class="field">
    <%= f.label :checkin %><br />
    <%= f.date_select :checkin %>
  </div>
  <div class="field">
    <%= f.label :checkout %><br />
    <%= f.date_select :checkout %>
  </div>
  <div class="field">
    <%= f.label :noOfPeople %><br />
    <%= f.number_field :noOfPeople %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
